{
  "entities": {
    "AuthorizedUser": {
      "title": "Authorized User",
      "description": "A user phone number that is permitted to access the application.",
      "type": "object",
      "properties": {
        "phoneNumber": { "type": "string", "description": "E.164 formatted phone number" },
        "name": { "type": "string", "description": "Name of the person (optional)" },
        "authorizedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["phoneNumber"]
    }
  },
  "firestore": {
    "authorized_users/{phoneNumber}": {
      "schema": "AuthorizedUser",
      "description": "List of phone numbers allowed to access the app."
    }
  }
}
